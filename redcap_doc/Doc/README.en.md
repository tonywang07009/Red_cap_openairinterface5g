# RedCap Stable Docs

## Purpose
- Stable RedCap manuals, checklists, spec notes, and paper notes live here.
- Generated logs do not belong here.

## Documentation Split
- API/config behavior: use spec notes or function reference pages.
- Bash usage: point to `redcap_interface/`.
- Step-by-step recap: keep reproducible commands and expected markers.

## Accuracy Rule
- Use exact local spec references when verified.
- Mark uncertain clause mappings as `[Needs Verification]`.
