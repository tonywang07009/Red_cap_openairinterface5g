# Spec Docs

## Purpose
- Local RedCap and 3GPP notes live here.
- Use this folder before writing spec claims in implementation docs.

## API Mapping Rule
- Cite exact local notes or exact clause numbers only after checking the source.
- If the mapping is not verified, write `[Needs Verification]`.

## Common Use
- RedCap capability notes.
- RRC/MAC low-power behavior notes.
- Project-specific spec summaries.
- L1/L2 protocol guide: `../redcap_l1_l2_protocol_guide.md`.
- L1-L3 function lookup: `../function_reference/redcap_l1_l3_function_lookup.md`.
