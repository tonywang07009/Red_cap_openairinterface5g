# Build Evidence Library 文件

## 用途
- 這裡放保留的 build 與 image rebuild evidence。

## Bash 連結
```bash
./mmtc.menu.bash rebuild
./mmtc.menu.bash inspect
```

## 規則
- 只保留與 runtime claim 或 project decision 有關的 build evidence。
