# Build Evidence Library

## Contents
| File | Role |
|---|---|
| `build_nr_uesoftmodem_redcap_pusch256qam_final.log` | Final UE softmodem build evidence for the PUSCH 256QAM capability path |
| `rebuild_local_oai_images_redcap_pusch256qam_final.log` | Final local image rebuild evidence for the same path |

## Usage
- Use these logs only as proof that the retained Paper 07 256QAM attempt used rebuilt UE/runtime images.
- New build logs should continue to be written to `test_log/build_logs/`.
- Promote only final build evidence that supports a report or runtime result.
