# Runtime Probe Library Docs

## Purpose
- Retained runtime probe evidence and reusable probe assets live here.

## Step-by-Step Recap
- Use `mmtc.menu.bash` for daily RFsim runs.
- Use `mmtc.display.bash` for visible iperf panels and paper demos.
- Store new generated logs in `test_log/` first.

## Rule
- Promote only stable final evidence into this folder.
