# Runtime Probe Library 文件

## 用途
- 這裡放保留下來的 runtime probe evidence 與可重用 probe asset。

## Step-by-Step Recap
- 日常 RFsim 使用 `mmtc.menu.bash`。
- 可視化 iperf panel 與 paper demo 使用 `mmtc.display.bash`。
- 新產生 log 先放 `test_log/`。

## 規則
- 只有穩定 final evidence 才 promote 到這裡。
