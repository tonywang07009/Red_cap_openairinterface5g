# Report Summary Library 文件

## 用途
- 這裡放已接受的 RedCap summary report。

## 撰寫規則
- 開頭先寫 result、command、evidence path。
- 舊 `test_log/` 路徑作為歷史證據 reference 保留。
- 新 report 使用目前指令。

## 目前指令偏好
- 日常 RFsim：根目錄 `mmtc.menu.bash`。
- Paper/demo：`redcap_interface/mmtc.display.bash`。
