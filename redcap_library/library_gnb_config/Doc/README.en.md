# gNB Config Library Docs

## Purpose
- Reusable final gNB config baselines live here.

## API/Config Behavior
- Default daily menu config: `gnb_redcap_mmtc_case_b_final.yaml`.
- PRB-specific active configs may still live in `ci-scripts/conf_files/`.

## Bash Link
```bash
./mmtc.menu.bash status
```
