# gNB Config Library 文件

## 用途
- 這裡放可重用的 final gNB config baseline。

## API/Config 行為
- 日常 menu 預設 config：`gnb_redcap_mmtc_case_b_final.yaml`。
- PRB-specific active config 仍可能放在 `ci-scripts/conf_files/`。

## Bash 連結
```bash
bash redcap_interface/mmtc.menu.bash status
```
