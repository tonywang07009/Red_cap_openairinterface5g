# Gate Reports

## Purpose

- Store final Gate reports for RedCap Workflow 3.0.
- Use `validation/gate_report_template.md` for each report.
- Link runtime logs or build logs by path instead of pasting raw output.

## Naming

- `G<gate>_<short_topic>_<YYYY-MM-DD>.md`

## Current Status

- No runtime Gate report exists yet.
- G0 scaffold evidence is covered by OpenSpec validation and the static checker.
