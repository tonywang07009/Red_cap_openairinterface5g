## 1. Existing-owner and runtime inventory

- [x] 1.1 Trace the existing control contract, native FlexRIC xApp helper, compose assets, and reusable Python helper; record the exact owner seams and unresolved live KPM facts without changing them.
- [x] 1.2 Select the smallest existing project location for the DRL workspace CLI, shared image definitions, bridge extension, and Gate guide; add Bash Tool Registry entries before adding executable automation.

## 2. Shared releases and workspace lifecycle

- [x] 2.1 Implement immutable local CPU and GPU DRL release builds with pinned release metadata and smoke checks for Python, PyTorch, Gymnasium, Stable-Baselines3, shared xApp library, and SWIG extension loading.
- [x] 2.2 Implement atomic named-workspace initialization with explicit workspace root, compose path, runtime variant, initial locked profile, image identity lock, read-only external configuration mounts, and refusal on existing names or unresolved compose data.
- [x] 2.3 Implement generated overlay lifecycle operations (`up`, `verify`, `down`, `remove`) that add only workspace services and preserve simulator services and artifacts.
- [x] 2.4 Implement explicit `build-release` and `upgrade` operations that preserve the old workspace lock on failure and do not issue E2 control.

## 3. Native bridge and observation gates

- [x] 3.1 Implement the versioned workspace-private UDS protocol for `health`, `open`, `observe`, `act`, and `close`, with schema validation and no public TCP or Docker socket exposure.
- [x] 3.2 Implement the minimum SWIG/native adapter that reuses the identified FlexRIC owner seam and preserves existing helper behavior until live validation proves it.
- [x] 3.3 Source-trace and test the existing gNB KPM-agent cell-capability/report seam. Extend only `ran_func_kpm.c`, `sm_ag_if_rd.h`, and the KPM v03.00 agent adapter as required to emit a separately identifiable cell stream while preserving the UE Style-4 path. Keep the cell delta baseline in the existing subscription-owned action object and release it through the existing `free_act_def` lifecycle; do not add a request-ID registry, global subscription table, new unsubscribe operation, or shared process-wide baseline. Record unresolved E2SM-KPM format or metric mappings as `[Needs Verification]`.
  - 2026-08-27: frozen setup and initial Format-1 cell-report tracers pass under the isolated CMake test build. One remaining frozen tracer shall cover valid second-and-later samples, first/invalid-delta no-value behavior, two independently owned baselines, and release through the existing action-definition free path. Identity retention and unsubscribe are not separate implementation slices.
  - 2026-08-28: `test_ran_func_kpm_cell_delta` recorded first/valid/invalid samples and two independent baseline objects. Historical SHA-256 is `740bbdf315895398833dbdef5824a2bb9c5e50b0d594428ad0d8dbda2cd14ba0`; RED and GREEN evidence are `test_log/compiler_logs/drl_xapp_kpm_cell_delta_{red,green}_2026-08-28.log`. All three isolated KPM tracers pass and are now normal `0644` Git/CI tests. `kpm_sm_static` also compiles with `KPM_V3_00`. Existing `free_act_def` remains the sole release path. `RRU.PrbTotDl`/`RRU.PrbTotUl` percentage semantics and live E2SM interoperability remain **[Needs Verification]**.
- [x] 3.4 Extend the existing FlexRIC SWIG seam with the minimum KPM subscription/callback projection required for Python-safe cell and UE observations; refuse unsupported styles or malformed callback data before bridge control.
  - 2026-08-28: Batch A Terra RED and Luna GREEN tracers cover primitive cell/UE callbacks, malformed callback refusal, target-binding refusal, and no-control behavior. Python suite passes 21/21. Native and generated SWIG wrappers pass syntax checks; full CMake SWIG build remains **[Needs Verification]** because the host has SWIG 4.0.2 while the project requires >=4.1.
- [x] 3.5 Implement `discover-kpm` evidence capture and profile qualification for separate cell and UE streams; refuse control when live capability, freshness, alignment, or verified target binding is absent.
  - Historical 2026-08-28: `qualify-kpm` performed `discover` through the UDS
    seam and wrote discovery plus qualification to one run manifest; live
    cell/UE evidence and E2 source provenance were then pending.
  - Historical 2026-08-28: Batch A subscribed to primitive streams and refused
    malformed callbacks or missing KPM-to-RC binding before control. Its
    projected `source_seq` was only a callback ordinal at that point.
  - 2026-08-28: An otherwise complete primitive UE binding without explicit
    `source_seq_origin=e2_indication` now refuses with
    `SOURCE_SEQUENCE_UNVERIFIED` at the binding gate. The RED/GREEN Python
    traces are `drl_xapp_task35_source_seq_{red,green}_2026-08-28.log`; GREEN
    passes 30/30. This adds no qualification success path or control attempt.
  - 2026-08-30: Decision recorded in `design.md`: `freshness_window_ms`,
    `cell_ue_max_skew_ms`, and `min_valid_paired_samples` are profile-owned,
    human-approved calibration parameters. Until live evidence is explicitly
    frozen, qualification remains observation-only and control fails closed.
  - 2026-08-31: Native periodic indications carry `RICindicationSN` through
    the xApp KPM handoff and SWIG callback. The live 1.0.14 observation run
    at node `2:1:1:3584` captured one paired cell/UE sample with
    `source_seq_origin=e2_indication`, `gnb-ran:1` / RC UE ID `1` / RNTI
    `12064`, 0 ms skew, and `control_attempted=false`. Qualification correctly
    stopped at `MEASUREMENT_POST_UNFROZEN`; retained manifest:
    `/tmp/task35-e2-sn-live/task35-e2-sn-live/artifacts/runs/20260831T112450Z-137662c5/manifest.json`.

## 4. Bounded control and recovery

- [x] 4.1 Implement node-level exclusive lease acquisition, `TARGET_BUSY`, durable control journal, and explicit recovery state handling.
  - 2026-08-28: Batch B Luna GREEN verifies atomic `LEASE_ACQUIRED` journaling, existing-node `TARGET_BUSY`, `ROLLBACK_UNCONFIRMED` lock, and explicit proved-baseline `recover` to `RECOVERED`. Full 26-test Python suite passes; live journal/provider integration remains **[Needs Verification]**.
- [x] 4.2 Implement `ul-prb-cap-v1` contract validation and the one-candidate baseline → candidate → baseline transaction, including acknowledgement, gNB apply marker, later KPM proof, and rollback lock behavior.
  - 2026-08-28: Batch B Luna GREEN verifies contract rejection at 276, acceptance at 275, exactly baseline/candidate/restore phases, required ACK/apply/later-KPM proof fields, and one best-effort restore on missing proof. Real native provider and gNB marker correlation remain **[Needs Verification]** for Task 6.3.
- [x] 4.3 Implement `run --enable-control` for the fixed/greedy validation controller and a `module:callable` model entrypoint; expose no generic multi-episode training syntax without a separately approved profile.
  - 2026-08-28: Batch C GREEN covers smoke-before-qualification and CLI-side `module:callable` refusal. The bounded transaction, generic multi-episode refusal wording, live binding, and candidate policy remain **[Needs Verification]**.
  - 2026-09-01: the model profile accepts one immutable 30-sample `RRU.PrbTotUl` summary, invokes `module:callable` once, and accepts only one strict `1..51` candidate before the existing bounded transaction. Focused contract tests pass 6/6 and the full Python suite passes 66/66; live collection and proof remain Tasks 6.2/6.3.
  - 2026-09-01: the temporary `--episodes` refusal syntax is removed. The CLI parser now rejects it before any workspace, evidence, runtime, or UDS activity; a multi-episode profile remains a separate future OpenSpec change. RED plus the public-CLI GREEN and full 66-test tracer are retained as `task43_remove_episodes_{red,public_green,public_full_green}_2026-09-01_*.log`.

## 5. Evidence and developer guidance

- [x] 5.1 Implement per-run manifests, event stream, KPM summaries, journal, generated-overlay evidence, resolved gNB marker excerpt, truthful gate status, and safe-next-command output.
  - 2026-08-28: Batch C GREEN projects qualified `node_id` and resolved compose/overlay evidence paths; control journal and gNB marker evidence remain unpopulated until a live transaction is attempted, so this task stays open.
  - 2026-09-01: `execute_control_run()` now retains one package from preflight
    through finalization, model evidence, collector, and UDS records. The
    runtime input mount is separate from the bridge UDS mount, and the generic
    runtime client is removed. Local suite passes 71/71 in
    `task51_architecture_full_green_final_2026-09-01_16-22-36.log`; live
    evidence remains Tasks 6.2/6.3.
- [x] 5.2 Add Traditional-Chinese read-only top-level and per-command help, including release-upgrade order, profile limits, lease behavior, discovery versus qualification, evidence meaning, and side effects.
- [x] 5.3 Add the model-invoked `redcap-drl-xapp-gates` guide using Gate-oriented progressive disclosure for model developers and profile maintainers.
- [x] 5.4 Consolidate change-private TDD contracts and execution records.
  - Move slice-level TDD contracts, RED/GREEN results, and implementation records from `design.md` into `tdd.md`.
  - Keep `design.md` limited to current architecture decisions, safety invariants, and a link to `tdd.md`.
  - Retain historic records, deduplicate identical workflow records, and add a supersession index.
  - Validation: referenced current logs exist and `openspec validate build-redcap-drl-xapp-gated-runtime --strict` passes.
  - Boundary: documentation-only; no Docker, E2 subscription, or E2SM-RC control.
  - 2026-09-01: migrated the prior 1,191-line `design.md` record into `tdd.md`, rebuilt `design.md` as a 152-line current-decision document, removed two duplicate workflow records, and added the current-contract plus supersession indexes. Referenced current logs exist; strict validation passed.

## 6. Validation and review

- [x] 6.1 Add and run narrow refusal-path tests for existing workspace names, unresolved compose data, unsupported bridge protocol, profile-none action, absent binding, target lease conflict, and unsafe rollback state.
- [x] 6.2 Run CPU and GPU release smoke tests, workspace overlay isolation checks, and a live discovery/qualification check against a user-supplied running FlexRIC scenario; record unavailable live KPM facts as `[Needs Verification]`.
  - [x] 6.2.1 Add an observation-only CPU KPM cadence probe with per-stream subscription/callback/indication evidence; diagnose `KPM_STREAM_EMPTY` without E2SM-RC control.
  - [x] 6.2.2 Keep qualified KPM subscriptions session-scoped, and prove a control phase only from a post-send pair on the retained stream. The native report period must be below the one-second proof window.
    - 2026-09-02: retained-stream/post-send local seam recheck passes 4/4, including one-second ACK/marker/KPM composition, one fresh pair under a 30-sample policy, no resubscription after native send, and the 100 ms native report-period assertion. At record time live native-phase proof remained pending; it is superseded by the approved live record below.
  - [x] 6.2.3 Collect representative stable calibration evidence. Model-summary sample count and measurement-post policy sample count remain distinct decisions.
  - 2026-08-18: release `1.0.1` CPU, RTX 5060 Ti CUDA tensor, and SWIG bridge smoke passed. The isolated overlay started and stopped without simulator service changes; runtime and bridge gates passed. RIC name resolution failed because the user-supplied RIC service was not running, so live discovery/qualification remains incomplete.
  - 2026-08-18: release `1.0.3` added Python-readable KPM report-style and RC control-style/action capability projection. Minimal live startup made the RIC healthy, but the compose-declared gNB image rejected the current YAML value `do_SRS: 1` because that image accepts only `none`, `periodic`, or `aperiodic`. The targeted RIC/gNB containers were removed; no KPM subscription or control was sent.
  - 2026-08-18: release `1.0.7` aligned the bridge with `E2AP_V3`/`KPM_V3_00` and projected node identity plus RAN-function IDs as Python-safe primitives. Live discovery passed for node `2:1:1:3584`, advertising KPM function 2, RC function 3, KPM report style 3, and RC control styles 1/2. KPM cell/UE stream qualification remains incomplete; no subscription or control was sent. The isolated workspace, targeted RIC/gNB containers, and temporary image override were removed.
  - 2026-08-18: release `1.0.8` added real qualification capability preflight. Live `qualify-kpm` refused with `CELL_KPM_STREAM_REQUIRED` at the capability stage because the node advertises only raw style tuple `(3,3,0,2)`, which maps to KPM Style 4 / Action Definition 4 / Indication Message 3 (UE-level). The OAI agent source only implements Format 4. No KPM subscription or E2SM-RC control was sent, and all targeted containers plus the temporary override were removed.
  - 2026-09-01: current-source release `1.0.15` passed CPU runtime, RTX 5060 Ti CUDA tensor, and SWIG bridge smoke; manifest is `test_log/runtime_configs/drl_releases/1.0.15.json` and the build log is `test_log/compiler_logs/task62_live_qualification_2026-09-01.log`. Isolated CPU/GPU workspaces were initialized under `test_log/runtime_configs/task62_workspaces/`; both overlays passed runtime, bridge-health, and RIC-name-resolution checks and point to distinct runtime image tags.
  - 2026-09-01: the user-supplied CN5G/FlexRIC/gNB/UE scenario reached E2 setup and UE registration. Read-only discovery passed for the single eligible node `2:1:1:3584`, with KPM/RC functions and the advertised cell/UE styles. CPU qualification captured one paired `e2_indication` sample with `gnb-ran:1`, RC UE ID `1`, RNTI `21870`, 0 ms cell/UE skew, and then refused at `MEASUREMENT_POST_UNFROZEN`; evidence is `test_log/runtime_configs/task62_workspaces/task62-cpu/artifacts/runs/20260901T090539Z-aef95327/manifest.json`. GPU discovery passed after a transient E42 setup timeout; repeated GPU qualification produced `KPM_STREAM_EMPTY`, so live callback cadence and cell/UE stream stability remain **[Needs Verification]**. No E2SM-RC control was sent.
  - 2026-09-01: an absolute workspace path exceeded Linux `AF_UNIX`'s 108-byte path limit; `uds_call()` now uses a temporary short symlink only for over-limit client paths, while control calls retain their five-second UDS timeout. The full Python tracer passes 72/72 in the same task log.
  - 2026-09-01: release `1.0.16` lowered native KPM reporting to 100 ms and added observation-only `probe-kpm`. CPU run `20260901T094918Z-2840effd` passed on node `2:1:1:3584`: cell/UE each had 3 callbacks, first callback 103 ms, and `RICindicationSN` 3; `control_attempted=false`. Thirty subsequent retained-stream qualifications stayed fail-closed at `MEASUREMENT_POST_UNFROZEN`, but recorded 30 temporal pairs for node `2:1:1:3584`/RNTI `9419`, sequences 476..1171, maximum freshness 1 ms, and skew 0 ms. Their UL PRB utilization is zero, so representative load coverage and human threshold approval remain **[Needs Verification]**. Evidence: `test_log/compiler_logs/task62_cadence_repair_2026-09-01.log` and `test_log/runtime_configs/task62_cadence_workspaces/task62-cpu-1.0.16/artifacts/runs/`.
  - 2026-09-01: bounded UE-to-external-DN UDP load confirms the cell metric is live. A 10 Mbit/s 12-second flow carried 10.5 Mbit/s at 0% loss but remained cell-PRB 0 due to integer percentage resolution. A 50 Mbit/s 12-second flow carried 52.4 Mbit/s at 0% loss; its 30 fail-closed qualifications captured cell `RRU.PrbTotUl` `0..37` (mean 7.2; 6 nonzero), max freshness 2 ms, max skew 1 ms, and `control_attempted=false`. This covers the candidate-16 operating region; 55%/80% greedy-boundary load remains **[Needs Verification]** and is unnecessary for the approved fixed candidate-16 transaction. Evidence: `test_log/compiler_logs/task62_cpu_load_calibration_2026-09-01.log` and `test_log/runtime_configs/task62_cadence_workspaces/task62-cpu-1.0.16/load_calibration_summary_20260901.json`.
  - 2026-09-01: `down` previously left `run/bridge.sock`, falsely making a stopped workspace ineligible for `freeze-measurement-post`. The successful lifecycle now unlinks that path only after Compose succeeds; failed shutdown preserves it. The focused RED/GREEN and 78/78 full tracer pass, and the stopped live workspace removed its stale socket without starting a container. Evidence: `test_log/compiler_logs/task62_socket_cleanup_2026-09-01.log`.
  - 2026-09-02: release `1.0.17` smoke passed for CPU/GPU/bridge. After a clean CN5G/FlexRIC/gNB/UE restart, the CPU `probe-kpm` passed at run `20260902T014627Z-af59fb39`, and frozen `qualify-kpm` passed at run `20260902T014636Z-2051229b` with 30 valid paired `e2_indication` samples, latest freshness 1 ms, and maximum cell/UE skew 0 ms under the approved `200/20/30` policy. The CPU qualification gate is proven; at record time the parent 6.2 checkbox remained open because fresh GPU live qualification was not repeated after the release restart. Evidence: `test_log/compiler_logs/task62_frozen_requalification_final_2026-09-02.log` and the workspace `artifacts/runs/` tree.
  - 2026-09-02: one fixed candidate-16 control run was attempted only after the CPU frozen gate passed. Native ACKs and gNB markers recorded `0 → 16 → 0`, but the client-side `act` timed out at five seconds before receiving the bridge result; `close` saw a reset connection. The run is fail-closed and remains Task 6.3 evidence, not success. Evidence: `test_log/compiler_logs/task63_bounded_control_2026-09-02.log` and run `20260902T014841Z-c3433b77`.
  - 2026-09-02: the local repair raised the enclosing UDS control budget to 20 seconds and wired `--teardown` to the existing `down` lifecycle. Focused RED/GREEN tests and the full 81-test Python tracer pass. No second live control was sent. Evidence: `test_log/compiler_logs/task63_orchestration_repairs_full_green_2026-09-02.log` and `test_log/compiler_logs/task63_teardown_green_2026-09-02.log`.
  - 2026-09-02: the GPU workspace was upgraded to immutable release `1.0.17` and qualified observation-only against a clean CN5G/FlexRIC/gNB/UE restart. The approved `200/20/30` measurement-post policy was frozen from 30 GPU calibration runs on node `2:1:1:3584`; the frozen qualification passed at run `20260902T063402Z-c24ac755` with 30 valid paired `e2_indication` samples, latest freshness `1 ms`, maximum cell/UE skew `0 ms`, verified RNTI `45242`, and `control_attempted=false`. A transient nearRT-RIC exit (`139`) was repaired before the final smoke/qualification; all task-started GPU, target, and CN5G services were removed afterward. This completes the CPU/GPU observation-only evidence; the later live 6.2.2 and Task 6.3 proof is recorded below. Evidence: `test_log/compiler_logs/task62_gpu_live_qualification_2026-09-02.log` and `test_log/runtime_configs/task62_workspaces/task62-gpu/artifacts/runs/20260902T063402Z-c24ac755/manifest.json`.
- 2026-09-02: the approved CPU release `1.0.17` run completed the native post-send proof for all three phases. `act` returned `ok=true` for baseline `0`, fixed candidate `16`, and restore `0`; the marker excerpt contains the same requested/effective sequence for RNTI `7651`, and the bridge retained the existing KPM subscriptions while enforcing the later-pair deadline. This closes the live portion of 6.2.2; the immutable run package and transcript are recorded under Task 6.3 below. Evidence: `test_log/compiler_logs/task63_bounded_control_approved_2026-09-02.log` and `test_log/runtime_configs/task62_cadence_workspaces/task62-cpu-1.0.16/artifacts/runs/20260902T071437Z-b5133c57/`.
- [x] 6.3 Run one fixed/greedy bounded control transaction only when all live gates pass; preserve contract validation, xApp acknowledgement, gNB apply marker, and later KPM evidence. Do not claim a full DRL training experiment.
  - 2026-09-01: not started. Task 6.2 did not produce a qualified, frozen measurement-post gate; the runner therefore sent no `open`, `act`, or `close` request and created no control-attempt evidence. A human-approved `freeze-measurement-post` calibration decision and a fresh live qualification are required before the single baseline→candidate→baseline transaction.
  - 2026-09-02 (superseded attempt): after the CPU Task 6.2 gate passed, `open` succeeded and native ACK/marker evidence exists for baseline `0`, candidate `16`, and restore `0`, but the five-second UDS client budget expired before `act` returned; `close` was reset. The bridge journal reached `COMPLETED`, while the run evidence lacked the `act` result object, so Task 6.3 stayed unchecked at that point. A 20-second timeout and `--teardown` repair then passed locally; the approved replacement run is recorded below.
  - 2026-09-02: after the approved 20-second UDS repair, exactly one new live run `20260902T071437Z-b5133c57` passed. `open`, `act`, and `close` all returned `ok=true`; `act` reported the baseline → fixed candidate `16` → restore transaction, the marker excerpt recorded `0 → 16 → 0` with requested/effective values for RNTI `7651`, and frozen qualification supplied 30 paired `e2_indication` samples under `200/20/30`. The run emitted one `CONTROL_RUN_STARTED` and one `CONTROL_RUN_FINISHED`, then `--teardown` removed only the selected workspace services; the target gNB/RIC and CN5G stacks were stopped afterward. Evidence: `test_log/compiler_logs/task63_bounded_control_approved_2026-09-02.log`, `test_log/runtime_configs/task62_cadence_workspaces/task62-cpu-1.0.16/artifacts/runs/20260902T071437Z-b5133c57/manifest.json`, `events.ndjson`, `kpm_evidence.json`, and `gnb_apply_excerpt.log`.
  - 2026-09-02: evidence audit found the historical package journal was the pre-control `OPEN_PENDING` snapshot while the workspace durable journal was `COMPLETED`. The finalized package was not rewritten because finalized run IDs are immutable. A TDD RED/GREEN repair now projects the latest valid workspace journal into future finalized packages while retaining `control_attempted=true`; the focused and full Python tracer pass 82/82. Evidence: `test_log/compiler_logs/task63_journal_finalization_red_2026-09-02.log`, `test_log/compiler_logs/task63_journal_finalization_green_2026-09-02.log`, and `test_log/compiler_logs/task63_journal_finalization_full_green_2026-09-02.log`. This repair was local and no second live control was sent.
- [x] 6.4 Run strict OpenSpec validation and separate standards/spec review from a fixed implementation point; resolve findings before marking the change complete.
  - 2026-08-18: strict OpenSpec validation passed after the release `1.0.7` discovery evidence update. Separate standards/spec review remains pending.
  - 2026-09-02: Task 6.4 RED/GREEN/full-GREEN evidence covers standalone package finalization, append refusal, manifest/event ordering, terminal workspace-journal validation, and truthful model artifact paths. The full tracer passes 84/84; strict validation and the post-fix fixed-point review are recorded below.
  - 2026-09-02: strict validation passed at `test_log/compiler_logs/task64_openspec_validate_2026-09-02.log`. Standards and spec reviews used fixed point `30439d86ba5f3801da0bc449181ef35e9bf605d4`; the standards hard finding on finalized-event bypass was resolved with private `_record_final_event()` and named post-send constants, and the spec soft finding on nonterminal journals was resolved with terminal-state validation plus a dedicated refusal test. No remaining hard findings. Historical commit hygiene and test data-clump observations remain documented non-blocking items; history was not rewritten.
