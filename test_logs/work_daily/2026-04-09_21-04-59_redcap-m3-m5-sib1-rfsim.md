---

# Work Daily Log

## Session Metadata
- Date: 2026-04-09 21:04
- Agent Session ID: N/A
- Task Slug: redcap-m3-m5-sib1-rfsim

## Milestone & Sub-task Reference
- Milestone: Milestone 3 [MAC / RRC Capability Enhancement] + Milestone 5 [Integration & Throughput Targets]
- Sub-task: [SIB1 RedCap initial DL/UL BWP wiring] + [UE RedCap SIB1 barring checks] + [RF simulator XML attach/throughput entry]
- Status: [COMPLETED]

## What Was Done
- [gNB config path] 讀入 `halfDuplexRedCapAllowed_r17` 與 `redCapInitialBWP_r17`，並依 [TS 38.306 Clause 4.2.21.1] 檢查 [FR1 20 MHz] 對應 PRB 上限。
- [SIB1 build path] 在 gNB 端寫入 `initialDownlinkBWP-RedCap-r17`、`initialUplinkBWP-RedCap-r17` 與 `halfDuplexRedCapAllowed-r17`。
- [UE config path] 從 `nrue_recap` YAML 載入最小 RedCap capability，並在 `rrc_UE.c` 依 `halfDuplexRedCapAllowed-r17` / `cellBarredRedCap1Rx-r17` / `cellBarredRedCap2Rx-r17` 決定是否允許啟動 RA。
- [UE MAC path] 若 UE 為 RedCap，優先套用 SIB1 中的 RedCap initial DL/UL BWP，而非沿用 common initial BWP。
- [Scenario path] 更新 `container_5g_flexric_rfsim_redcap.xml`，加入 `302003` gNB log 驗證，並補 `030001` / `030002` 的 RedCap iperf case。
- [Build wiring] 在 `CMakeLists.txt` 將 `lib_uicc` 接到 `nr-softmodem`，避免 `nr_parms.c` 解析 `load_nr_redcap_config()` 時出現 undefined reference。
- [Offline build helper] 在 `cmake_targets/CPM.cmake` 加入「若本地已有 CPM 檔案則跳過下載」的保護，降低 offline sandbox 下的 configure 風險。

## 3GPP Spec Clauses Referenced
- TS 38.306 Clause 4.2.21.1 — [RedCap UE] 在 [FR1] 最大頻寬為 [20 MHz]，並限制 [DL layers] 與 [UL Tx branch]。
- TS 38.331 Clause 5.2.2.4.2 — [half-duplex FDD RedCap UE] 在 `halfDuplexRedCapAllowed-r17` 缺省時需把 cell 視為 [barred]；`cellBarredRedCap1Rx-r17` / `cellBarredRedCap2Rx-r17` 直接影響接入。
- TS 38.331 Clause 5.6.1.3 — UE 收到 `UECapabilityEnquiry` 後，需要回傳 `UECapabilityInformation` / `UE-NR-Capability`。
- TS 38.331 SIB1 ASN.1 — `RedCap-ConfigCommonSIB-r17`、`initialDownlinkBWP-RedCap-r17`、`initialUplinkBWP-RedCap-r17` 為本次 SIB1 wiring 的核心欄位。

## Test Results
| Test Item | Pass / Fail | Coverage | Notes |
|-----------|-------------|----------|-------|
| `xmllint --noout ci-scripts/xml_files/container_5g_flexric_rfsim_redcap.xml` | Pass | N/A | XML parser 可讀取新場景入口 |
| `docker compose -f ci-scripts/yaml_files/5g_rfsimulator_flexric_redcap/docker-compose.yml config -q` | Pass | N/A | compose 語法與 service wiring 正常 |
| `ctest --test-dir cmake_targets/ran_build/build_test -R 'test_nr_frame_params|test_nr_ue_power_procedures|test_nr_ue_ra_procedures' --output-on-failure` | Pass | N/A | 3 個既有單元測試全數通過 |
| `cmake --build --preset tests --target nr-softmodem nr-uesoftmodem test_nr_frame_params test_nr_ue_power_procedures test_nr_ue_ra_procedures` | Fail | N/A | 第一次失敗於 `nr-softmodem` link：`load_nr_redcap_config` 未解析；後續已補 `lib_uicc` wiring |
| `cmake --preset tests -DCPM_SOURCE_CACHE=...` | Fail | N/A | offline sandbox 下 `CPMAddPackage` / `~/.cache/cpm` reconfigure 仍有 blocker |

## Known Issues / Blockers
- [Full source-level rebuild blocker] `ENABLE_TESTS=ON` 的 configure 仍受 [CPM / googletest] 本地 cache 與 offline sandbox 影響；修改 `CMakeLists.txt` 後會觸發 CMake regenerate，暫時卡在 `CPMAddPackage` 路徑。
- [Full E2E blocker] 尚未實跑 [Core + gNB + UE + FlexRIC] attach/ping/iperf，因此 `302003` / `030001` / `030002` 尚未做 runtime 驗證。

## Next Step
- 先穩定 [offline CMake/CPM] configure 路徑，讓 `nr-softmodem` 能用新的 `lib_uicc` link 設定完成正式重建。
- 完成後實跑 `container_5g_flexric_rfsim_redcap.xml`，收集 [attach log]、[gNB RedCap detection]、[302003]、[030001]、[030002] 結果。
