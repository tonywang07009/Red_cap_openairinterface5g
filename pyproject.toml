[project]
name = "red-cap-openairinterface5g"
version = "0.1.0"
description = "Python tooling for the RedCap OpenAirInterface5G workspace"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
  "paramiko",
  "PyYAML",
]
